# Dependencies for the IsaacLab-GR00T automation pipeline (pipeline_runner.py & modules).
# pegasus.py already requires: requests, truststore, websocket-client, urllib3.
paramiko>=3.0      # SSH/SFTP to the asus-4090 sim box (password auth, cross-platform)
PyYAML>=6.0        # config.yaml
rich>=13.0         # live training progress in the terminal
