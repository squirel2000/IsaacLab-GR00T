# Shared Project Datasets

This directory is for project-level fine-tuning/evaluation datasets shared by
GR00T, StarVLA, and future VLA backends.

Do not move upstream package demo datasets, simulator assets, or local examples
here unless they become shared project datasets.
